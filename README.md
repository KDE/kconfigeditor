A graphical editor for KConfig files.

This application is an editor for application's config files, based on KConfigXT schemas. Therefore it only works for configuration where such schemas exist.

Since every Flatpak app has its own config folder the UI separates things per-application.

This project is under heavy development. All Rust crimes are artisanal and hand-made.
